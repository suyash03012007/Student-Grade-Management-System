# Student-Grade-Management-System
Academic Management System Technology using Python

📌 Features

1. Student Management

    * Add, search, update, and delete student records
    * Soft delete (mark inactive) or hard delete (permanent)
    * Automatic student ID generation (STU001, STU002, ...)
    * Email uniqueness + validation
    * Age validation (16–100 years)

2. Grade Management

    * Add, update, delete grade entries
    * View grades per student
    * Percentage & letter grade calculation
    * Supports assignments, quizzes, midterms, finals, projects

3. Calculations

    * Subject-wise average
    * Semester GPA
    * Cumulative GPA (CGPA)
    * Grade-to-point mapping (A+, A, B+, ...)

4. Reporting Module

    * Individual Student Report (console, text file, Excel)
    * Class Performance Report
    * Subject Performance Report
    * Semester Summary Report
    * Top performers

5. Data Storage

    * Local text files (pipe-delimited):

    -`students.txt`
    - `grades.txt`
    * Automatic backup before every write
    * Deletion log stored in `deletion_log.txt`

6. Excel Import/Export

    * Uses `openpyxl`
    * Import/export students
    * Import/export grades
    * Export formatted reports

7. User Interface

    * Fully menu-driven
    * Clean, readable console layout
    * Validation and error handling on all inputs

---

🛠 Requirements

    * Python **3.x**
    * `openpyxl` library

Install dependencies:

```
pip install openpyxl
```

---

▶️ How to Run the Program

1. Download the project file: `student_grade_management_system.py`
2. Ensure text files exist (the program will auto-create if missing):

   * `students.txt`
   * `grades.txt`
   * `deletion_log.txt`
3. Run the program:

```
python student_grade_management_system.py
```

4. Navigate using the on-screen menus:

    * Student Management
    * Grade Management
    * Reports
    * Import/Export Data

---

📂 File Structure

```
project_folder/
│
├── student_grade_management_system.py
├── students.txt
├── grades.txt
├── deletion_log.txt
└── backups/
```

---

📘 Main Modules (Inside the Single File)

Student Functions

    * `add_student()`
    * `search_student_by_id()`
    * `update_student()`
    * `delete_student()`
    * `generate_next_student_id()`

Grade Functions

    * `add_grade()`
    * `update_grade()`
    * `delete_grade()`
    * `get_student_grades()`

Calculations

    * `calculate_percentage()`
    * `get_letter_grade()`
    * `calculate_semester_gpa()`
    * `calculate_cgpa()`
    * `calculate_subject_average()`

Reports

    * `individual_student_report()`
    * `class_performance_report()`
    * `subject_performance_report()`
    * `semester_summary_report()`

Excel Utilities

    * `export_students_to_excel()`
    * `export_grades_to_excel()`
    * `import_students_from_excel()`
    * `import_grades_from_excel()`

---

📄 Data Format Examples

students.txt

```
STU001|Sanket|Dodya|info@example.com|15/08/2005|Computer Science|2023|Active
```

grades.txt

```
STU001|Python Programming|Assignment|85.0|100.0|10/10/2024|Fall 2024
```

---

🧪 Error Handling

    * Duplicate Student ID → Suggests next available ID
    * Missing files → Auto-creates new empty files
    * Invalid DOB / Email / Marks → Shows descriptive errors
    * Excel import → Skips faulty rows and continues

---

🧩 Future Improvements (Optional)

    * Move to SQL database (SQLite / MySQL)
    * Add GUI (Tkinter) or Web dashboard (Flask/FastAPI)
    * Add role-based login (Admin/Teacher)
    * Add analytics charts/visualization

---

👨‍💻 Author

Developed under the requirements document by **Sanket Dodya**.
This Python implementation generated by **ChatGPT**.

---

✔️ End of README
